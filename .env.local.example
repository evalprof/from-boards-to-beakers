# Phase 2 — set these in Vercel project settings (and locally in .env.local for dev)

# Supabase — server-side database access (service role bypasses RLS)
NEXT_PUBLIC_SUPABASE_URL=https://ppukvlevivjjlimcxqxe.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ... (from Supabase dashboard → Project Settings → API)

# Resend — email notifications on new submissions
RESEND_API_KEY=re_...           (from resend.com/api-keys)
RESEND_FROM_EMAIL=onboarding@resend.dev    (default until you verify a custom domain)
SUBMISSIONS_NOTIFY_EMAIL=naturewarrior11@gmail.com
